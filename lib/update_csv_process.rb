class UpdateCsvProcess
    def run(request)
        # result = []
        # CSV.parse(File.read(request.update_table), headers: true) do |row|
        #     if row.to_hash["name"] != "Neil Ranada"
        #         result << row.to_hash
        #     end
        # end
        # puts result

        # CSV.open(request.update_table, "w+", :row_sep => "\r\n", headers: true)

        # CSV.open(request.update_table, "a+", :row_sep => "\r\n", headers: true) do |csv|
        #     result.each do |row|
        #         csv << row.values
        #     end
        # end
    end
end
